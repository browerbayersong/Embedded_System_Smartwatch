**项目分工与交付（基于 STM32CubeMX + VSCode）**

本文件为三人项目的精细分工、交付清单与立刻可执行的下一步。请按各自分支开发并提交 PR 合并到 `develop`。

**总体说明**
- **开发环境**: 使用 `STM32CubeMX` 生成 `project.ioc`，在 `VSCode` 中以 `Makefile`/`CMake` + `arm-none-eabi-gcc` 开发。
- **版本控制**: 每人使用 feature 分支，PR 合并到 `develop`。

**方旭亮 — 硬件负责人（分支: feature/hw-schematic）**
- 交付：`/hw/schematic.pdf`、`/hw/BOM.xlsx`、`/hw/self_test.md`
- 任务要点：
  - 设计 STM32F103 最小系统原理图（复位、晶振、SWD、供电、一键开机）。
  - 设计 I2C 总线（OLED、MPU6050）接口、上拉与去耦。
  - 设计 LDO 电源与 Type-C 充电保护电路。
  - 焊接最小系统并提交硬件自检：上电电压、I2C 扫描截图或日志。

**陈斯焕 — 固件/RTOS 负责人（分支: feature/firmware-freertos）**
- 交付：`/firmware/project.ioc`、`/firmware/Src/*`、`/firmware/Inc/*`、`/firmware/Makefile`
- 任务要点：
  - 在 `STM32CubeMX` 中配置时钟、I2C、USART、定时器、FreeRTOS。
  - 实现 `i2c_drv.c/.h`、`mpu6050.c/.h`、`oled.c/.h`。
  - 实现基本 FreeRTOS 任务：`task_time.c`、`task_sensor.c`、`task_ui.c`。
  - 提供 VSCode 编译与调试配置（`.vscode/tasks.json`、`launch.json` 或 README）。

**宋柏粤 — 交互/算法/通信（分支: feature/ui-bluetooth）**
- 交付：`/app/menu.c`、`/app/pedometer.c`、`/docs/protocol.md`、`/docs/ui_mockups.md`
- 任务要点：
  - 实现旋转编码器驱动与多级菜单 (`encoder.c/.h`, `menu.c`)；UI 交互逻辑与 mockups。
  - 实现基础计步算法并保存运动数据（`pedometer.c/.h`）。
  - 集成蓝牙模块并定义同步协议，提供上位机/手机协议文档与示例脚本。

**PR/合并与代码规范**
- 每个 PR 必须包含：变更说明、编译/跑步测步骤、验证结果截图或日志、已知问题。
- 建议统一接口约定与代码布局（由组员 B 提供 `/firmware/README_dev.md` 模板）。

**立刻可执行的下一步（由三人并行）**
1. 组员 A：开始画最小系统原理图并生成初版 `BOM.xlsx`。
2. 组员 B：在 CubeMX 新建工程并提交初始 `project.ioc`（启用 I2C、USART、FreeRTOS）。
3. 组员 C：准备 UI mockups（`/docs/ui_mockups.md`）并列出菜单项优先级。

**建议分配（首周重点）**
- 快速验证原型：A 完成原理图 + 焊接最小系统（Breadboard/PCB）；B 完成 I2C 驱动与 MPU6050 读取；C 完成 OLED 时间页与编码器基本交互。

